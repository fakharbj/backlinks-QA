# ── Web / API ────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
gunicorn==22.0.0
pydantic>=2.10.0          # 2.7.x pydantic-core has no Python 3.13 wheel
pydantic-settings==2.3.4
python-multipart==0.0.9
email-validator==2.2.0

# ── Database ────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.31
asyncpg>=0.30.0          # 0.29 has no Python 3.13 wheel and won't compile
alembic==1.13.2
greenlet>=3.1.0  # 3.0.x fails to build on Python 3.13

# ── Auth / Security ─────────────────────────────────────────────────────────
argon2-cffi==23.1.0
pyjwt==2.8.0
cryptography==42.0.8

# ── Queue / Workers ─────────────────────────────────────────────────────────
celery==5.4.0
redis==5.0.7
flower==2.0.1
celery-redbeat>=2.3.2     # 2.2.0 not published for current setuptools

# ── Crawler ─────────────────────────────────────────────────────────────────
httpx[http2]==0.27.0
# Content-Encoding decoders for httpx. brotli is the DEFAULT on most CDNs/
# Cloudflare; without it httpx passes the compressed bytes through undecoded and
# the HTML parser sees 0 links → false "link missing" verdicts. zstandard covers
# the newer `zstd` encoding. gzip/deflate are stdlib.
brotli>=1.1.0
zstandard>=0.22.0
lxml>=5.3.0              # 5.3.0 is the first with Python 3.13 wheels
tldextract==5.1.2
idna==3.7
charset-normalizer==3.3.2
# Optional headless-render add-on (RENDER_ENABLED=true). Not installed by default
# — Playwright + its browsers are heavy and unsupported on some Python 3.13 builds.
# playwright==1.45.0

# ── Reports / Exports ───────────────────────────────────────────────────────
openpyxl==3.1.5
fpdf2==2.7.9              # pure-Python PDF (no Pango/Cairo system libs needed)
jinja2==3.1.4

# ── Object storage ──────────────────────────────────────────────────────────
boto3==1.34.144

# ── Google Sheets ───────────────────────────────────────────────────────────
gspread==6.1.2
google-auth==2.32.0

# ── Observability ───────────────────────────────────────────────────────────
structlog==24.2.0
prometheus-client==0.20.0
sentry-sdk==2.7.1

# ── Testing (kept here so the worker image can self-test; see requirements-dev) ─
playwright>=1.61  # headless render escalation (Chromium via 'playwright install chromium')
